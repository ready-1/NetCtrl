# coding: utf-8

"""
M4300 REST API

M4300 REST API with ConfigAgent Documentation.

The version of the OpenAPI document: 2.0.0.59
Generated by OpenAPI Generator (https://openapi-generator.tech)

Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import (
    BaseModel,
    ConfigDict,
    Field,
    StrictBool,
    StrictInt,
    StrictStr,
    field_validator,
)
from typing import Any, ClassVar, Dict, List
from typing import Optional, Set
from typing_extensions import Self


class Download(BaseModel):
    """
    Download
    """  # noqa: E501

    protocol: StrictStr = Field(description="Protocol to by used for file transfer")
    fw_url: StrictStr = Field(
        description="tftp, http or https link to firmware image or configuration file to be upgrade.",
        alias="fwUrl",
    )
    fw_download_timeout: StrictInt = Field(
        description="Firmware iamge download time-out value in seconds from http or https link.",
        alias="fwDownloadTimeout",
    )
    reboot_now: StrictBool = Field(
        description="Indicates when to reboot:   * `true` = immediate reboot   * `false` = reboot manually by admin ",
        alias="rebootNow",
    )
    type: StrictStr = Field(
        description="Type of file being downloaded to the switch:   * `backup-config` = Configuration file saved to the backup configuration settings   * `firmware` = Firmware image saved to the specified image number. "
    )
    image: StrictInt = Field(
        description="Specify image firmware will be downloaded:   * `1` = Image 1   * `2` = Image 2 "
    )
    __properties: ClassVar[List[str]] = [
        "protocol",
        "fwUrl",
        "fwDownloadTimeout",
        "rebootNow",
        "type",
        "image",
    ]

    @field_validator("protocol")
    def protocol_validate_enum(cls, value):
        """Validates the enum"""
        if value not in set(["tftp", "http", "https"]):
            raise ValueError("must be one of enum values ('tftp', 'http', 'https')")
        return value

    @field_validator("type")
    def type_validate_enum(cls, value):
        """Validates the enum"""
        if value not in set(["backup-config", "firmware"]):
            raise ValueError("must be one of enum values ('backup-config', 'firmware')")
        return value

    @field_validator("image")
    def image_validate_enum(cls, value):
        """Validates the enum"""
        if value not in set([1, 2]):
            raise ValueError("must be one of enum values (1, 2)")
        return value

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )

    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of Download from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of Download from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate(
            {
                "protocol": obj.get("protocol"),
                "fwUrl": obj.get("fwUrl"),
                "fwDownloadTimeout": (
                    obj.get("fwDownloadTimeout")
                    if obj.get("fwDownloadTimeout") is not None
                    else 600
                ),
                "rebootNow": (
                    obj.get("rebootNow") if obj.get("rebootNow") is not None else True
                ),
                "type": obj.get("type"),
                "image": obj.get("image"),
            }
        )
        return _obj
